# Kalebs Tracker

Internal multi-tenant task management web app.

## Getting Started

```bash
pnpm install
```

Copy `.env.local.example` to `.env.local` and fill in your Supabase credentials:

```bash
cp .env.local.example .env.local
```

Then start the development server:

```bash
pnpm dev
```
